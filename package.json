{
  "name": "ilheu-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "test": "node --test tests/*.test.mjs",
    "product:add": "node scripts/add-product.mjs",
    "shipping:setup": "node scripts/setup-shipping.mjs",
    "shipping:restore": "node scripts/restore-issue-b-shipping.mjs",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/mdx": "^4.3.13",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^5.18.0",
    "lightningcss": "^1.32.0",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "wrangler": "^4.114.0"
  }
}
