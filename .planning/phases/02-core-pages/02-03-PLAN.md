---
phase: 02-core-pages
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/editions/index.astro
  - src/pages/editions/[editionId].astro
  - src/components/EditionCard.astro
  - src/components/ArticleTeaser.astro
  - src/components/ContributorCard.astro
autonomous: true
requirements: [EDIT-03, EDIT-04]

must_haves:
  truths:
    - "Visitor can browse /editions and see all editions listed with themes and cover thumbnails"
    - "Editions are sorted by edition_number descending (newest first)"
    - "Visitor can click an edition and land on /editions/[editionId] with theme hero, cover, article list, contributors, and buy link"
    - "Edition page articles are presented as an editorial table-of-contents list (title, author, excerpt)"
    - "Edition page contributors are shown as small portraits with names and roles"
    - "Contributors without portrait images show a fallback initial/avatar"
    - "Buy CTA is prominent near the top of the edition page when purchase_url exists"
  artifacts:
    - path: "src/pages/editions/index.astro"
      provides: "Edition archive listing page"
      min_lines: 30
    - path: "src/pages/editions/[editionId].astro"
      provides: "Individual edition page with theme, cover, articles, contributors, buy CTA"
      min_lines: 60
    - path: "src/components/EditionCard.astro"
      provides: "Reusable edition card with cover thumbnail and theme"
      min_lines: 15
    - path: "src/components/ArticleTeaser.astro"
      provides: "Article row for edition TOC list"
      min_lines: 15
    - path: "src/components/ContributorCard.astro"
      provides: "Contributor portrait + name + role with fallback"
      min_lines: 15
  key_links:
    - from: "src/pages/editions/[editionId].astro"
      to: "astro:content"
      via: "getStaticPaths + getCollection for articles filtered by edition ID prefix"
      pattern: "id\\.startsWith\\(edition\\.id"
    - from: "src/pages/editions/[editionId].astro"
      to: "astro:content"
      via: "getEntries to resolve contributor references"
      pattern: "getEntries.*contributors"
    - from: "src/pages/editions/index.astro"
      to: "src/components/EditionCard.astro"
      via: "component import"
      pattern: "import EditionCard"
---

<objective>
Build the edition archive page and individual edition landing page. Visitors can browse all editions and drill into a specific edition to see its articles, contributors, and buy link.

Purpose: Editions are the primary content unit. The archive shows breadth, the individual page shows depth — like a table of contents inside a print magazine.
Output: Archive at /editions, individual pages at /editions/[editionId], plus three reusable components (EditionCard, ArticleTeaser, ContributorCard).
</objective>

<execution_context>
@/Users/filipemoura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filipemoura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- Edition schema (from content.config.ts) -->
Edition fields: edition_number, title, theme, release_date, cover_image (optional), accent_colour (optional), purchase_url (optional), status ('current' | 'archive'), flipbook_embed_url (optional), contributors (optional array of contributor refs)

Article fields: title, author (contributor ref), published_date, excerpt, cover_image (optional), featured (boolean), tags, language, type enum

Contributor fields: name, role, bio, portrait (optional), linked_articles (optional)

<!-- Key patterns -->
```typescript
// Edition dynamic route
export async function getStaticPaths() {
  const editions = await getCollection('editions');
  return editions.map(edition => ({
    params: { editionId: edition.id },
    props: { edition },
  }));
}

// Filter articles for an edition (MUST include trailing slash to prevent prefix collision)
const articles = await getCollection('articles', ({ id }) =>
  id.startsWith(edition.id + '/')
);

// Resolve contributor references
const contributors = edition.data.contributors
  ? await getEntries(edition.data.contributors)
  : [];

// Resolve single article author
const author = await getEntry(article.data.author);
```

<!-- Sample data -->
Edition 1 (archive): "Sao Miguel" / "Island of Origins" — 3 articles, 3 contributors
Edition 2 (current): "The Nine" / "Across the Archipelago" — 3 articles, 3 contributors
Article IDs: "edition-1/articles/hugo-goncales-interview", "edition-2/articles/joao-medeiros-essay", etc.
Contributor portraits: all optional (none exist in placeholders)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable components (EditionCard, ArticleTeaser, ContributorCard)</name>
  <files>src/components/EditionCard.astro, src/components/ArticleTeaser.astro, src/components/ContributorCard.astro</files>
  <action>
    Create three reusable components in src/components/:

    **EditionCard.astro** — Used on archive page and homepage archive preview:
    - Props: edition (the collection entry object)
    - Renders: cover thumbnail (Image component, guarded for optional), edition title, theme, edition_number
    - Links entire card to `/editions/${edition.id}`
    - Cover image: use `import { Image } from 'astro:assets'`; if no cover_image, show a styled placeholder div with the edition number
    - Responsive: reasonable card sizing for grid layout

    **ArticleTeaser.astro** — Used on edition page as TOC-style row:
    - Props: article (collection entry), authorName (string)
    - Renders: article title (font-cardo), author name, excerpt (truncated to 2-3 lines)
    - Title links to `/articles/${article.id}`
    - Table-of-contents feel: clean, vertical list, generous line spacing
    - Per user decision: "title, author, excerpt per row, vertical"

    **ContributorCard.astro** — Used on edition page:
    - Props: contributor (collection entry)
    - Renders: portrait image or fallback initial, name, role
    - Portrait fallback: if no portrait image, show a circle div with the first letter of the contributor's name (bg-gray-200, text-gray-500)
    - Per user decision: "small portraits with names and roles"
    - Size: compact (w-12 h-12 or similar for portrait, inline layout)
  </action>
  <verify>
    <automated>cd /Users/filipemoura/dev/ilheu/website-2.0 && ls src/components/EditionCard.astro src/components/ArticleTeaser.astro src/components/ContributorCard.astro && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Three component files exist with typed Props interfaces. Build passes (components are valid Astro even if not yet used in pages).</done>
</task>

<task type="auto">
  <name>Task 2: Build edition archive page and individual edition page</name>
  <files>src/pages/editions/index.astro, src/pages/editions/[editionId].astro</files>
  <action>
    **src/pages/editions/index.astro (Archive — EDIT-03):**
    - Fetch all editions: `await getCollection('editions')`
    - Sort by edition_number descending (newest first)
    - Render page title "Editions" with editorial styling
    - Display each edition using the EditionCard component
    - Layout: responsive grid (1 col mobile, 2 cols md+) with generous gaps
    - Wrap in BaseLayout with title "Editions — Ilheu Magazine"

    **src/pages/editions/[editionId].astro (Individual edition — EDIT-04):**
    - Use getStaticPaths to generate one page per edition
    - Fetch edition's articles: filter by `id.startsWith(edition.id + '/')`
    - Resolve contributors: `edition.data.contributors ? await getEntries(edition.data.contributors) : []`
    - For each article, resolve the author: `await getEntry(article.data.author)`

    Layout per user decisions:
    1. **Theme hero** (top): Large theme text (font-cardo, text-3xl lg:text-5xl) with cover image alongside
       - Desktop: side-by-side (text leading, cover supporting)
       - Mobile: stacked (theme text above, cover below)
       - Edition title and edition_number as secondary text
    2. **Buy CTA**: Prominent button near the top, near theme/cover area. Links to purchase_url when present; hidden entirely when absent
    3. **Articles list** (table-of-contents style): Use ArticleTeaser component for each article. Vertical list, clean editorial feel. Sort articles by published_date or as-authored order
    4. **Contributors** section: Use ContributorCard component for each contributor. Horizontal flex wrap layout for the portrait+name cards

    IMPORTANT: Use `entry.id` not `entry.slug` (Astro v5 pattern). Use `import { render } from 'astro:content'` not `entry.render()`.
    IMPORTANT: Article IDs contain slashes. Links should be `/articles/${article.id}`.
    IMPORTANT: Guard all optional image fields with conditionals before passing to Image component.
  </action>
  <verify>
    <automated>cd /Users/filipemoura/dev/ilheu/website-2.0 && npm run build 2>&1 | tail -3 && ls dist/editions/index.html dist/editions/edition-1/index.html dist/editions/edition-2/index.html</automated>
  </verify>
  <done>Build produces /editions/index.html (archive with both editions), /editions/edition-1/index.html and /editions/edition-2/index.html (each with theme hero, article list, contributor cards, and buy CTA when purchase_url exists).</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- dist/editions/index.html exists and contains both edition titles
- dist/editions/edition-1/index.html contains "Island of Origins" theme and 3 article titles
- dist/editions/edition-2/index.html contains "Across the Archipelago" theme and 3 article titles
- Contributor names appear on edition pages
- EditionCard, ArticleTeaser, ContributorCard components are imported and used
</verification>

<success_criteria>
Visitors can browse all editions at /editions and see covers+themes. Clicking an edition shows its full editorial contents: theme hero, article table-of-contents, contributors, and buy CTA. The experience feels like opening a magazine's inside cover.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-03-SUMMARY.md`
</output>
