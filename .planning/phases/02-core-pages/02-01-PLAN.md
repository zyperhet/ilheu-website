---
phase: 02-core-pages
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/styles/global.css
  - package.json
  - package-lock.json
autonomous: true
requirements: [NAV-01, TECH-03]

must_haves:
  truths:
    - "Site has a persistent header with centered magazine logo and 4 nav items (Editions, Articles, Shop, About)"
    - "Navigation scrolls with the page (not sticky)"
    - "Mobile users (375px+) see a hamburger menu that toggles the nav items"
    - "Desktop users see horizontal nav items below the logo"
    - "Every page has a consistent footer"
    - "Rendered Markdown content has proper typographic styles via prose class"
    - "Headings use Cardo font, body text uses Inter font"
  artifacts:
    - path: "src/components/Nav.astro"
      provides: "Site navigation with logo, 4 items, mobile hamburger"
      min_lines: 30
    - path: "src/components/Footer.astro"
      provides: "Site footer"
      min_lines: 10
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with Nav and Footer integrated"
      min_lines: 20
    - path: "src/styles/global.css"
      provides: "Tailwind imports, typography plugin, Google Fonts, custom theme"
      min_lines: 8
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Nav.astro"
      via: "component import"
      pattern: "import Nav from.*Nav\\.astro"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "component import"
      pattern: "import Footer from.*Footer\\.astro"
    - from: "src/styles/global.css"
      to: "@tailwindcss/typography"
      via: "@plugin directive"
      pattern: "@plugin.*@tailwindcss/typography"
---

<objective>
Build the base layout shell with persistent navigation, footer, typography plugin, and custom fonts. This is the foundation every other page in Phase 2 will use.

Purpose: Every page needs the same nav, footer, font stack, and prose styling. Building this first means all subsequent plans just wrap their content in BaseLayout and get consistent chrome.
Output: Updated BaseLayout.astro with Nav and Footer components, global.css with typography plugin and Cardo/Inter fonts.
</objective>

<execution_context>
@/Users/filipemoura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filipemoura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md

<interfaces>
<!-- Existing BaseLayout that will be extended -->
From src/layouts/BaseLayout.astro:
```astro
---
import '../styles/global.css';
interface Props {
  title?: string;
}
const { title = 'Ilheu Magazine' } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
```

From src/styles/global.css:
```css
@import "tailwindcss";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install typography plugin and configure fonts and theme</name>
  <files>package.json, package-lock.json, src/styles/global.css</files>
  <action>
    1. Install @tailwindcss/typography:
       ```
       npm install -D @tailwindcss/typography
       ```

    2. Update src/styles/global.css to:
       ```css
       @import "tailwindcss";
       @plugin "@tailwindcss/typography";
       @import url('https://fonts.googleapis.com/css2?family=Cardo:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600&display=swap');

       @theme {
         --font-cardo: 'Cardo', serif;
         --font-inter: 'Inter', sans-serif;
       }
       ```

    3. Verify build still passes with `npm run build`.

    NOTE: Tailwind v4 uses @plugin directive in CSS, NOT a tailwind.config.js plugins array. This project has no tailwind.config.js by design.
  </action>
  <verify>
    <automated>cd /Users/filipemoura/dev/ilheu/website-2.0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>@tailwindcss/typography installed, global.css has @plugin directive, Google Fonts import for Cardo+Inter, @theme block with --font-cardo and --font-inter custom properties. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Nav, Footer, and update BaseLayout</name>
  <files>src/components/Nav.astro, src/components/Footer.astro, src/layouts/BaseLayout.astro</files>
  <action>
    1. Create src/components/Nav.astro with:
       - Centered magazine name "Ilheu" (using font-cardo, tracking-widest, uppercase) as a link to "/"
       - Below: 4 nav items — Editions (/editions), Articles (/articles), Shop (/shop), About (/about)
       - Desktop (md+): horizontal flex row, centered, separated by gaps, uppercase tracking-wider text-sm, with subtle top and bottom border
       - Mobile (<md): hamburger button (inline SVG, positioned top-right), toggles a vertical nav menu
       - Hamburger toggle via a minimal `<script>` tag: getElementById('menu-btn') toggles hidden class on mobile-menu
       - Navigation is NOT sticky — it is static, scrolls with the page
       - Add active-state styling: compare Astro.url.pathname against each link's href, apply underline or font-weight to current page

    2. Create src/components/Footer.astro with:
       - Simple centered footer with magazine name, copyright year, and a subtle top border
       - Small text: "Ilheu Magazine" and "A biannual magazine from the Azores"
       - Keep it minimal and editorial — this is not a mega-footer

    3. Update src/layouts/BaseLayout.astro:
       - Import Nav and Footer components
       - Add `<Nav />` above the `<slot />`
       - Add `<Footer />` below the `<slot />`
       - Set base body classes: font-inter (default body font), text-gray-900, bg-white, antialiased
       - Keep the existing Props interface (title prop)
       - Add a `<meta name="description">` tag with a default description prop

    4. Verify the layout renders correctly:
       ```
       npm run build
       ```
       Check that dist/index.html contains the nav items and footer text.

    Per user decisions:
    - 4 nav items exactly: Editions, Articles, Shop, About
    - Centered logo/name above nav — editorial/print magazine feel
    - Static navigation — NOT sticky
    - Mobile: hamburger (Claude's discretion, chosen for clean mobile UX)
  </action>
  <verify>
    <automated>cd /Users/filipemoura/dev/ilheu/website-2.0 && npm run build 2>&1 | tail -3 && grep -c "Editions" dist/index.html && grep -c "footer" dist/index.html</automated>
  </verify>
  <done>Nav.astro renders centered "Ilheu" logo + 4 nav items (desktop horizontal, mobile hamburger toggle). Footer.astro renders minimal editorial footer. BaseLayout.astro imports both, applies font-inter body text. Build passes and dist/index.html contains nav and footer markup.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- dist/index.html contains all 4 nav links (Editions, Articles, Shop, About)
- dist/index.html contains footer content
- global.css has @plugin "@tailwindcss/typography" and @theme block with font definitions
- @tailwindcss/typography appears in package.json devDependencies
</verification>

<success_criteria>
Every page using BaseLayout gets a consistent header (centered logo + 4-item nav), footer, Cardo/Inter fonts, and prose styling for rendered Markdown. Mobile users see a hamburger menu. The layout is not sticky.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-01-SUMMARY.md`
</output>
