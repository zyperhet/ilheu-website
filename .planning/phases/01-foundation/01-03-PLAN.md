---
phase: 01-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .planning/phases/01-foundation/DEPLOYMENT.md
autonomous: false
requirements:
  - TECH-02

must_haves:
  truths:
    - "A visitor can reach a live URL on Cloudflare Pages and see the skeleton site"
    - "The site on Cloudflare Pages renders the same page as the local build"
    - "The Cloudflare Pages build uses Node.js 22 (matching .nvmrc)"
    - "The live URL is documented so the team can access it"
  artifacts:
    - path: ".planning/phases/01-foundation/DEPLOYMENT.md"
      provides: "Deployment details: live URL, Cloudflare project name, build settings"
  key_links:
    - from: ".nvmrc"
      to: "Cloudflare Pages build system"
      via: "Cloudflare Pages reads .nvmrc for Node.js version"
      pattern: "\\.nvmrc"
    - from: "astro.config.mjs output: static"
      to: "Cloudflare Pages dist/ directory"
      via: "Build command: npm run build; Output: dist"
---

<objective>
Deploy the skeleton Astro site to Cloudflare Pages and verify the live site serves the correct content using the same Node.js version as local development.

Purpose: Prove the build-and-deploy pipeline works end-to-end before any real content is authored. Catches environment parity issues early.
Output: A live Cloudflare Pages URL serving the skeleton site; documented deployment settings.
</objective>

<execution_context>
@/Users/filipemoura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filipemoura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify local build and git state before deployment</name>
  <files>(git operations only — no new files)</files>
  <action>
    1. Verify the local build still passes cleanly with all content in place:
       ```bash
       cd /Users/filipemoura/dev/ilheu/website-2.0
       npm run build
       ```
       Must exit 0. If it fails, diagnose and fix before proceeding.

    2. Check that all files from plans 01-01 and 01-02 are committed:
       ```bash
       git status
       git log --oneline -5
       ```
       If there are uncommitted changes, commit them before deploying.

    3. Verify the remote origin is configured (Cloudflare Pages will need a git repository URL):
       ```bash
       git remote -v
       ```
       If no remote is configured, the user will need to create the GitHub repository and push — record this as a prerequisite in the checkpoint task.

    4. Verify `.nvmrc` contains `22` and is committed:
       ```bash
       cat .nvmrc
       git show HEAD:.nvmrc 2>/dev/null || echo "NOT COMMITTED"
       ```
  </action>
  <verify>
    <automated>cd /Users/filipemoura/dev/ilheu/website-2.0 && npm run build 2>&1 | tail -5 && echo "Build exit: $?" && git status && cat .nvmrc</automated>
  </verify>
  <done>`npm run build` exits 0; all plan 01-01 and 01-02 files are committed; .nvmrc is committed with "22"; git remote is configured OR user has been informed they must set one up</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Cloudflare Pages and verify live site</name>
  <what-built>
    The skeleton Astro site is built locally and all code is committed to git. Now it needs to be deployed to Cloudflare Pages.

    Cloudflare Pages deployment requires a Cloudflare account. The user said "deployment setup deferred — user will set up account when ready" and "staging domain first (not ilheumagazine.com directly)."

    To complete this task, the user must:

    1. Log in to Cloudflare Pages (https://pages.cloudflare.com)

    2. Connect the GitHub repository to a new Cloudflare Pages project

    3. Configure build settings exactly as follows:
       - Build command: `npm run build`
       - Build output directory: `dist`
       - Root directory: (leave blank — the root of the repo)
       - Node.js version: (leave blank — Cloudflare Pages will read `.nvmrc` automatically)
       - Framework preset: `Astro` (if prompted)

    4. Trigger the first deployment and wait for it to complete

    5. Once deployed, visit the live URL (format: `https://[project-name].pages.dev`)

    6. Confirm the live site shows "Ilhéu Magazine" and "Site under construction"

    7. In the Cloudflare Pages build logs, confirm the Node.js version is 22.x (matches `.nvmrc`)

    8. Record the live URL and Cloudflare project name for the summary

    If the deployment fails:
    - Check the build logs in Cloudflare Pages dashboard for errors
    - Common issue: Node.js version mismatch — ensure Cloudflare Pages is using v3 build system (Pages Settings > Build > Build system version > v3)
    - Common issue: Wrong build output directory — must be `dist`, not `build` or `public`
  </what-built>
  <how-to-verify>
    1. Visit the live `.pages.dev` URL in a browser
    2. Confirm the page shows: "Ilhéu Magazine" heading and "Site under construction" text
    3. Confirm in Cloudflare Pages build logs: Node.js version shows 22.x
    4. Optionally: Run `curl -s https://[your-project].pages.dev | grep "Ilhéu"` to verify from CLI
  </how-to-verify>
  <resume-signal>
    When the live URL is confirmed working, share:
    - The live URL (e.g., https://ilheu-website.pages.dev)
    - The Cloudflare Pages project name
    - The Node.js version shown in build logs

    Type "deployed: [URL]" to resume, or describe any deployment issues encountered.

    If you want to defer Cloudflare Pages setup and just verify local build parity instead, type "defer-deploy" and the plan will be marked complete with a note that deployment is pending.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document deployment configuration</name>
  <files>.planning/phases/01-foundation/DEPLOYMENT.md</files>
  <action>
    After the checkpoint is cleared (deployment confirmed working), create a deployment record file. This documents the live environment for the team and for future phases.

    If the user provided deployment details at the checkpoint, use those. Otherwise, use placeholders.

    Create `.planning/phases/01-foundation/DEPLOYMENT.md`:
    ```markdown
    # Deployment: Ilhéu Magazine Website 2.0

    **Deployed:** [DATE from checkpoint]
    **Environment:** Staging (Cloudflare Pages)

    ## Live URL

    [URL from checkpoint, e.g., https://ilheu-website.pages.dev]

    ## Cloudflare Pages Configuration

    | Setting | Value |
    |---------|-------|
    | Project name | [from checkpoint] |
    | Build command | `npm run build` |
    | Output directory | `dist` |
    | Root directory | (repo root) |
    | Node.js version | 22 (via .nvmrc) |
    | Build system | v3 |

    ## Environment Parity

    | Environment | Node.js | Build | Output |
    |-------------|---------|-------|--------|
    | Local | 22 (via .nvmrc) | `npm run build` | `dist/` |
    | Cloudflare Pages | 22 (via .nvmrc) | `npm run build` | `dist/` |

    ## Notes

    - No Cloudflare adapter installed — site is fully static (`output: 'static'` in astro.config.mjs)
    - Production domain (ilheumagazine.com) not configured yet — using staging .pages.dev URL
    - Custom domain setup deferred to when production launch is ready
    ```
  </action>
  <verify>
    <automated>test -f /Users/filipemoura/dev/ilheu/website-2.0/.planning/phases/01-foundation/DEPLOYMENT.md && echo "DEPLOYMENT.md EXISTS" && cat /Users/filipemoura/dev/ilheu/website-2.0/.planning/phases/01-foundation/DEPLOYMENT.md</automated>
  </verify>
  <done>DEPLOYMENT.md exists with live URL, build configuration, and Node.js version parity documented</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` exits 0 locally
2. Live Cloudflare Pages URL is accessible and shows the skeleton site
3. Cloudflare Pages build logs show Node.js 22.x
4. DEPLOYMENT.md is committed with live URL and configuration
5. Phase 1 success criteria are met:
   - Visitor can reach a live URL on Cloudflare Pages and see the skeleton site
   - Build works locally and on Cloudflare Pages from the same source
   - Node.js version pinned and matched in both environments
</verification>

<success_criteria>
- Live URL on Cloudflare Pages serves "Ilhéu Magazine" skeleton page
- Local `npm run build` and Cloudflare Pages build use identical Node.js 22 runtime
- Deployment configuration documented in DEPLOYMENT.md
- Phase 1 is complete: content schema locked, skeleton deployed, pipeline proven
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` using the summary template at `@/Users/filipemoura/.claude/get-shit-done/templates/summary.md`
</output>
